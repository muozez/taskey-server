worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid       /var/run/nginx.pid;

events {
    worker_connections 1024;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # ── Logging ──────────────────────────────────────────────
    log_format main '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent" '
                    'rt=$request_time uct=$upstream_connect_time uht=$upstream_header_time urt=$upstream_response_time';

    access_log /var/log/nginx/access.log main;

    # ── Performance ──────────────────────────────────────────
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # ── Gzip ─────────────────────────────────────────────────
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/json
        application/javascript
        application/xml
        image/svg+xml;

    # ── Security Headers ─────────────────────────────────────
    server_tokens off;

    # ── Upstream — load balance across app replicas ──────────
    # Docker DNS resolves "app" to all container IPs when scaled.
    # `least_conn` distributes to the replica with fewest active connections.
    upstream taskey_app {
        least_conn;
        server app:3000;
    }

    # ── Rate limiting ────────────────────────────────────────
    # Genel API istekleri için
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
    # Sync endpoint'leri için daha yüksek limit
    limit_req_zone $binary_remote_addr zone=sync:10m rate=60r/s;
    # Login / auth endpoint'leri için daha düşük limit (brute-force koruması)
    limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/s;

    # ── Server (HTTP → HTTPS redirect + health) ──────────────
    server {
        listen 80;
        server_name _;

        # Health check endpoint (for external LB / monitoring)
        location /health {
            access_log off;
            return 200 '{"status":"ok"}';
            add_header Content-Type application/json;
        }

        # Redirect all other HTTP traffic to HTTPS
        # NOTE: If running behind a trusted TLS-terminating load balancer,
        #       you can remove the HTTPS server block and keep this as the main server.
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # ── Server (HTTPS with TLS termination) ───────────────────
    # NOTE: Provide SSL certificates at deployment or remove this block
    #       if TLS is terminated at an external load balancer.
    server {
        listen 443 ssl http2;
        server_name _;

        # TLS configuration — provide certificates at deployment
        ssl_certificate     /etc/nginx/certs/server.crt;
        ssl_certificate_key /etc/nginx/certs/server.key;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;

        # Max body size for sync payloads
        client_max_body_size 10m;

        # Health check endpoint over HTTPS
        location /health {
            access_log off;
            return 200 '{"status":"ok"}';
            add_header Content-Type application/json;
        }

        # Auth endpoints — stricter rate limiting
        location ~ ^/api/(login|setup)$ {
            limit_req zone=auth burst=10 nodelay;

            proxy_pass http://taskey_app;
            proxy_http_version 1.1;

            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 10s;
            proxy_send_timeout    30s;
            proxy_read_timeout    30s;
        }

        # Sync endpoints — higher rate limit
        location /api/sync/ {
            limit_req zone=sync burst=120 nodelay;

            proxy_pass http://taskey_app;
            proxy_http_version 1.1;

            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 10s;
            proxy_send_timeout    60s;
            proxy_read_timeout    60s;
        }

        # All other API routes
        location / {
            limit_req zone=api burst=60 nodelay;

            proxy_pass http://taskey_app;
            proxy_http_version 1.1;

            # Connection
            proxy_set_header Connection "";
            proxy_set_header Host $host;

            # Real client IP forwarding
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Timeouts
            proxy_connect_timeout 10s;
            proxy_send_timeout    30s;
            proxy_read_timeout    30s;

            # Security headers
            add_header X-Content-Type-Options "nosniff" always;
            add_header X-Frame-Options "SAMEORIGIN" always;
            add_header X-XSS-Protection "1; mode=block" always;
            add_header Referrer-Policy "strict-origin-when-cross-origin" always;
            add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self';" always;
            add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
            add_header X-Permitted-Cross-Domain-Policies "none" always;
        }
    }
}
